# ~/.tmuxinator/Invoicing.yml

name: Invoicing
root: ~/repositories/projects/invoicing/

windows:
    - Editor:
        layout: main-horizontal
        panes:
            - workon Invoicing && vim -c "call ConfigureProjectInvoicing()"
            - workon Invoicing
    - Editor:
        layout: main-horizontal
        panes:
            - workon Invoicing
            - workon Invoicing
    - Editor:
        layout: main-horizontal
        panes:
            - workon Invoicing
            - workon Invoicing
    - Editor:
        layout: main-horizontal
        panes:
            - workon Invoicing
            - workon Invoicing
    - Editor:
        layout: main-horizontal
        panes:
            - workon Invoicing
            - workon Invoicing
    - Shell:
    - Shell:
    - Shell:
    - Shell:
    - Vagrant:
        layout: even-vertical
        panes:
            - cd vagrant
            - cd vagrant
            - git fm && exit
