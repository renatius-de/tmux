# ~/.tmuxinator/ShopRocket.yml

name: ShopRocket
root: ~/repositories/cbn/shoprocket/

windows:
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureShopRocket()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Shell:
    - Shell:
    - Shell:
    - Shell:
    - Vagrant:
        layout: tiled
        panes:
            - cd vagrant && vagrant up
            - cd vagrant
            - git fm && getComposer && exit
