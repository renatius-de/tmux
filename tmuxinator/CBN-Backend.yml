# ~/.tmuxinator/Backend.yml

name: CBN-Backend
root: ~/repositories/cbn/preisvergleich-backend/

windows:
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNBackend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNBackend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNBackend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNBackend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNBackend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Vagrant:
        layout: even-vertical
        panes:
            - git fm && getComposer && exit
            -
            -
