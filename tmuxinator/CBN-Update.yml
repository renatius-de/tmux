# ~/.tmuxinator/update.yml

name: CBN-Update
root: ~/repositories/cbn/preisvergleich-update/

windows:
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNUpdate()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNUpdate()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNUpdate()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNUpdate()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNUpdate()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Vagrant:
        layout: even-vertical
        panes:
            - git fm && getComposer && exit
            -
            -
