# ~/.tmuxinator/Preisvergleich.yml

name: CBN-Frontend
root: ~/repositories/cbn/preisvergleich-frontend/

windows:
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNFrontend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNFrontend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNFrontend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNFrontend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            - vim -c "call ConfigureCBNFrontend()"
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Editor:
        layout: main-horizontal
        panes:
            -
            -
    - Vagrant:
        layout: even-vertical
        panes:
            - git fm && getComposer && exit
            -
            -
